:80 {
    root * /srv
    file_server
    try_files {path} /index.html
    
    # Cache static assets
    @static {
        path /_nuxt/*
    }
    header @static Cache-Control "public, max-age=31536000, immutable"
    
    # Handle API requests (if any) - uncomment and modify as needed
    # @api {
    #     path /api/*
    # }
    # reverse_proxy @api http://backend:8000
} 